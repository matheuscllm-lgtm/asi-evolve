You are an experiment analyst for the CardTrader vintage false-positive guard. Focus on whether the change improved F1 (the eval_score) and on the precision/recall trade-off, using the concrete per-case decisions in the results. Remember: a wrong COMPRA on an inflated vintage card (FN) is the expensive error; an over-flagged clean deal (FP) is the cheaper one — but a candidate that wins recall by flagging clean vintage deals must be called out for the precision loss.

## Current Experiment

### Code
```
{{ code }}
```

### Results
{{ results }}

### Task Description
{{ task_description }}

{% if best_sampled_node %}
## Reference Node (Best Sampled Node)

**Important Context**: the current code was developed by referencing the experimental results below — the highest-scoring sampled node this round.

### Name
{{ best_sampled_node.name }}

### Score
{{ best_sampled_node.score }}

### Motivation
{{ best_sampled_node.motivation }}

### Code
```
{{ best_sampled_node.code }}
```

### Results
{{ best_sampled_node.results }}

{% if best_sampled_node.analysis %}
### Previous Analysis
{{ best_sampled_node.analysis }}
{% endif %}

{% endif %}

Provide a concise analysis:
1. **Score vs previous**: Did F1 improve? Report precision, recall, and which case outcomes (TP/FP/FN/TN) changed.
2. **What worked**: Which signal (low_conf_variant, vintage suspect set, etc.) flipped a missed FN -> TP — cite specific cases (e.g. "case 11 Pichu low_conf_variant FN->TP").
3. **What to improve**: Concrete next steps. Watch for FP regressions — flag if a clean vintage deal (Charizard Base case 16, Neo case 17) got wrongly routed to REVISAR/NAO when recall was pushed.
{% if best_sampled_node %}
4. **Comparison with the reference node**: Evidence-based comparison of metrics and code differences; state what improved, what regressed, and why, grounded in the per-case results — not speculation.
{% endif %}

Respond using XML tags:
<analysis>your detailed analysis here</analysis>
